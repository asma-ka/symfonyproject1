{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}

<div class="row">
                        <div class="col-12">
                            <div class="card">
                                <div class="card-header">
        
                                </div>
                                <div class="card-content">
                                    <div class="card-body">
            
    {{ form_widget(form) }}
    
     </div>
     </div>
     </div>
<button type="submit" class="btn btn-primary float-right">{{ button_label|default('Save') }}</button>
    
       
    
    </div>
    </div>
{{ form_end(form) }}
