{% extends 'base.html.twig' %}

{% block title %}Prestation{% endblock %}

{% block body %}
    <h1>Prestation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prestation.id }}</td>
            </tr>
            <tr>
                <th>NumeroPrestation</th>
                <td>{{ prestation.numeroPrestation }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ prestation.titre }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ prestation.genre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prestation_index') }}">back to list</a>

    <a href="{{ path('prestation_edit', {'id': prestation.id}) }}">edit</a>

    {{ include('prestation/_delete_form.html.twig') }}
{% endblock %}
